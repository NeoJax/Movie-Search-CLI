const expect = require('chai').expect;
const http = require('http');
const cheerio = require('cheerio');
const {
  parseHTML,
  queryIMDB
} = require('./movie-search.js');

describe('#parseHTML', function () {

  it('Checks whether the HTML is being called correctly', function() {
    
  })

  it('Checks whether the program is outputting the correct information for findingnemo.', function() {

  })

  it('Checks whether the program is outputting the correct information for harrypotter.', function() {

  })

  it('Checks whether the program is outputting the correct information for a blank space.', function() {

  })
})
